package edu.uchicago.gerber._06design.R12_11;

import java.util.List;

public class CountryStatistics  {
    private List<Country> countries;

    /**
     * Reads country data from the specified file.
     * @param filename Path to the file containing country records.
     */
    public void readCountryData(String filename) {
        // method implementation
        return;
    }

    /**
     * Finds and returns the country with the largest area.
     * @return Country with the largest area.
     */
    public Country findCountryWithLargestArea() {
        // method implementation
        return new Country("name", 1000, 10);
    }

    public Country findCountryWithPopulationDensity() {
        // method implementation
        return new Country("name", 1000, 10);
    }

    public Country findCountryWithPopulation() {
        // method implementation
        return new Country("name", 1000, 10);
    }

}
